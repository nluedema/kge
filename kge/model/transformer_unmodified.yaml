import: [lookup_embedder]

transformer_unmodified:
  class_name: TransformerUnmodified
  entity_embedder:
    type: lookup_embedder
    dim: 320
    +++: +++
  relation_embedder:
    type: lookup_embedder
    dim: 320
    +++: +++

  # Initialization for transformer layers.
  initialize: normal_
  initialize_args:
    mean: 0.0
    std: 0.02
    +++: +++