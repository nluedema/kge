import: [lookup_embedder]

transformer_david:
  class_name: TransformerDavid
  round_dim: False
  entity_embedder:
    type: lookup_embedder
    dropout: 0.0
    dim: 320
    +++: +++
  relation_embedder:
    type: lookup_embedder
    dropout: 0.0
    dim: 320
    +++: +++

  # Initialization for transformer layers.
  #initialize: 'xavier_uniform_'
  #initialize_args:
  #  +++: +++