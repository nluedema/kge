import: [lookup_embedder]

multimodal_embedder:
  class_name: MultimodalEmbedder
  dim: 200
  +++: +++                          # allow to add different modalities
  dropout: 0.
  regularize_args:
    weighted: False